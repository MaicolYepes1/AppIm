<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppIm.Views.OportunidadesPage"
             BackgroundColor="{StaticResource BackgroundColor}"
             BindingContext="{Binding Main, Source={StaticResource Locator}}"
             Title="Oportunidades">
    <ContentPage.Content>
        <StackLayout Padding="10,50">
            <Label TextColor="{StaticResource FontColor}"
                   FontSize="Medium"
                   Text="Información que se actualiza diariamente con
                   la ayuda de nuestro departamento de investigaciones
                   y telemercadeo, en donde mediante encuestas dicha empresa manifiesta el deseo de adquirir nuevos proveedores y qué tiene en cuenta dicha empresa a la hora de analizar nuevas propuestas. Este ítem nos muestra razón social, contacto, cantidad de operaciones 
                   tanto en impo como en expo, ciudad, teléfono y dirección. El número de operaciones es calculado con base al último año."
                   VerticalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>